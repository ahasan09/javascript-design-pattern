const division = (a, b) => {
    return a / b;
}

const add = (a, b) => {
    return a + b;
}

const subtract = (a, b) => {
    return a - b;
}

const multilpy = (a, b) => {
    return a * b;
}

const modulePattern = {
    add, subtract, multilpy, division
}

export default modulePattern;